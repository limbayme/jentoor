/* eslint-disable @next/next/no-img-element */
import { PageShell, Breadcrumbs, InquiryCta, pageMetadata } from '../_catalog/shared';
import { applications,getProduct } from '../_catalog/data';
export const metadata=pageMetadata('Supplement Application Directions','Explore daily wellness, beauty, digestive wellness, sports nutrition, relaxation and focus product development directions.','/solutions');
export default function SolutionsPage(){return <PageShell><section className="shell jt-page-hero"><Breadcrumbs items={[{name:'Applications'}]}/><p className="jt-kicker">A STARTING POINT, SHAPED AROUND PEOPLE</p><h1>Start with a need.<br/><em>Explore the possibilities.</em></h1><p className="jt-intro">Use these development directions to navigate the catalog. Each project still needs its own ingredient, serving, market and evidence review.</p></section><section className="shell jt-section jt-solution-grid">{applications.map(a=><a href={`/solutions/${a.slug}`} key={a.slug}><img src={getProduct(a.image)!.image} width="840" height="840" alt={`${a.name} illustrative product concept`} loading="lazy"/><div><p className="jt-kicker">{a.name}</p><h2>{a.title}</h2><p>{a.description}</p><span className="jt-link">Explore the direction ↗</span></div></a>)}</section><InquiryCta/></PageShell>;}
